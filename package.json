{
  "name": "json-to-csv",
  "version": "1.0.0",
  "description": "json to csv converter to csv in single line form json object arr",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Krzysztof Bogusz",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^1.18.10"
  }
}
